const AvlTree = @import("avl_tree.zig").AvlTree;
const IntervalTree = @import("interval_tree.zig").IntervalTree;
const LinkedHeap = @import("linked_heap.zig").LinkedHeap;

comptime {
    _ = AvlTree;
    _ = IntervalTree;
    _ = LinkedHeap;
}
